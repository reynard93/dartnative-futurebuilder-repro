# DartNative FutureBuilder disposal reproduction

## Scaffolded `dn run` demo (2026-09-23)

The repository now contains a complete app scaffold (`lib/`, `ios/`, and
`android/`) generated by `dn create`, in addition to the original regression
test. This addresses the requested runnable-demo workflow in
[DartNative/dartnative#42](https://github.com/DartNative/dartnative/issues/42).

With a working DartNative SDK installation and a configured iOS or Android
target, run from the repository root:

```sh
git clone https://github.com/reynard93/dartnative-futurebuilder-repro.git
cd dartnative-futurebuilder-repro
dn run
```

No `dn create`, manifest edits, application credentials, or separate test command
are needed to start the demo. `dn run` resolves the dependencies and generates
machine-local build configuration. With multiple devices, select the target when
prompted or use `dn run -d <device-id>` (see `dn devices`). Keep the default debug
mode: the reported failure is an assertion. Physical iOS devices require the
developer's own signing setup; no personal signing team is committed.

### What happens

The demo runs automatically once on launch. It first mounts a `FutureBuilder`
with a pending `Completer<int>`. At five seconds the parent removes the
`FutureBuilder` from the real widget tree. At nine seconds it completes the
pending future. Press **R** in the `dn run` terminal to repeat via hot restart.

The app uses normal framework mounting and disposal, not the internal lifecycle
hooks used by the headless test. The parent's timers check `mounted` and are
cancelled when the parent is disposed. No application handler catches or
suppresses the SDK exception.

On the verified SDK, the console reports:

```text
ISSUE42: Removing FutureBuilder from the real widget tree.
ISSUE42: Completing the pending future after removal.
Unhandled Exception: ... setState() called after dispose()
State.setState (package:dartnative/src/core.dart:166:12)
_FutureBuilderState._subscribe.<anonymous closure> (package:dartnative/src/widgets/builders.dart:325:9)
```

The screen displays the lifecycle steps, not an injected error or a pass/fail
verdict. The console trace is the evidence of the SDK failure. After an SDK
correction the same sequence should finish without that exception, and the
unchanged headless regression test should pass.

### Verified scope and evidence

Verified on **iPhone 17 Pro simulator, iOS 26.5, arm64**, using DartNative
`3.45.0-0.1.pre`, framework revision **`277f9a8c93`**, Dart
`3.12.0-192.0.dev`. Both the initial launch and a recorded hot restart reproduced
the assertion. Android scaffolding is included, but Android has not been built
or run for this update. No error-completion or future-replacement coverage is
claimed.

- [Screen recording — H.264 MP4 replacement](evidence/demo-ios-2026-09-23-compatible.mp4)
- [Matching console excerpt and unhandled exception](evidence/dn-run-ios-2026-09-23.txt)
- [SDK version](evidence/sdk-version-2026-09-23.txt)
- [Unchanged headless test failing on this SDK](evidence/failing-test-2026-09-23.txt)
- [Demo source](lib/main.dart)

The replacement recording is H.264/AVC Main Profile Level 4.0 (`avc1`) at
720×1566 / 30 fps with `yuv420p` pixel format in an MP4 container branded
`mp42`, with the metadata moved to the front of the file for progressive playback. The console excerpt omits unrelated simulator
startup/indexing and licence-status messages; the reproduction markers and
exception are unchanged. The original test file is byte-for-byte unchanged from
commit `78b5bc11e89e`. SDK binaries, build output, local package configuration,
and application files from other projects are not included.

---

## Original headless reproduction (2026-09-20)

This section preserves the original test-only report and its older SDK identity.
The current app and recording are documented above.

Minimal runnable reproduction for [DartNative/dartnative#42](https://github.com/DartNative/dartnative/issues/42).

A pending future completes after its `FutureBuilder` state is disposed. The SDK's own callback calls `setState()` and throws `setState() called after dispose()`.

## Run

Install the [DartNative SDK](https://dartnative.com/docs/getting-started/) and make `dn` available on PATH. Use `dn`, not plain `dart test` or Flutter: the test must run the compiled DartNative implementation rather than its declaration stubs.

```sh
git clone https://github.com/reynard93/dartnative-futurebuilder-repro.git
cd dartnative-futurebuilder-repro
dn pub get --no-example
dn test --no-pub --no-test-assets --reporter expanded test/future_builder_lifecycle_test.dart
```

No phone, emulator, application credentials, plugin registration, or native platform project is needed. This is an SDK lifecycle test, not a visual demo app.

## Result

On the tested SDK, the test **fails intentionally as a regression reproduction**, with exit code 1:

```text
setState() called after dispose()
package:dartnative/src/core.dart 166:12              State.setState
package:dartnative/src/widgets/builders.dart 325:9   _FutureBuilderState._subscribe.<fn>
```

After an SDK fix, the same test should pass unchanged with exit code 0. It does not catch or accept the assertion as success.

- [Captured failing test output](evidence/failing-test.txt)
- [SDK version and compiled build identity](evidence/sdk-version.txt)
- [Complete reproduction source](test/future_builder_lifecycle_test.dart)

Captured on macOS arm64, 2026-09-20, framework revision `9689ca2d76`, compiled SDK stamp `db1e0369d1dbdb3d`. Only the temporary checkout path in the test output is normalized to `<repro-root>`.

## Scope

The test creates the real SDK `FutureBuilder` state and drives its lifecycle through `stateSetWidget` and `stateSetMounted` from `package:dartnative/src/core.dart`: mounted initialization, disposal, unmounting, then completion of a `Completer`. These internal hooks are deliberate test machinery, not an application workaround. The test itself never calls `setState()`.

This confirms the success-after-disposal callback failure. It does not claim device navigation coverage, error-completion coverage, or future-replacement coverage. No screenshots are included: there is no UI surface in this reproduction, and the actual test output provides the relevant evidence.

Only the reproduction, manifest, instructions, and text evidence are published. SDK binaries, generated package configuration, local paths, and application files are excluded. The test package version is pinned; transitive dependencies resolve through the installed SDK and pub because DartNative SDK-local package paths are machine-specific.
